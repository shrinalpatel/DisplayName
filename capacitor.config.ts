import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lab01_mapd722',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
